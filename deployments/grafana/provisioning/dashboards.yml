apiVersion: 1

providers:
  - name: 'astigo dashboards'
    orgId: 1
    folder: 'Astigo'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards

  - name: 'loki dashboards'
    orgId: 1
    folder: 'Loki'
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/loki